# Analysis framework dependencies (distribution_tests/ + hallucination_agreement.py).
# Install into any Python 3.12 environment:  pip install -r requirements.txt
numpy
scipy
pandas
matplotlib
seaborn

# vlm_judge/ additionally requires the model stack from the repo root
# (torch, transformers, qwen-vl-utils; see ../requirements.txt). Running the
# open-weight VLM judges needs a CUDA-capable GPU.
