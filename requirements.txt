# Runtime dependencies.  Versions pinned conservatively to the ranges the
# training/inference code is known to work with — adjust if your
# environment requires newer ones.
torch>=2.1
transformers>=4.46
accelerate>=0.30
qwen-vl-utils>=0.0.10
peft>=0.10
Pillow>=10.0
pandas>=2.0
tqdm>=4.65
numpy>=1.24
